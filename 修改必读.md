# 修改必读

修改本项目之前，请先完整阅读本文档。项目由静态网页、FastAPI 后端、知识库资料和 Render 部署配置共同组成，只改其中一部分可能造成线上功能失效。

## 一、必须遵守的事项

1. 不得把 API Key、密码、Token 或其他私密信息写入代码、README、截图、提交记录或 `render.yaml`。
2. DeepSeek API Key 只能保存在：
   - 本机的 `agent-service/.env`；
   - Render 后台的 Secret 环境变量 `DEEPSEEK_API_KEY`。
3. 不得提交以下本地文件或目录：
   - `agent-service/.env`
   - `hf-cache/`
   - `milvus-data/`、`milvus-data-test/`
   - `runtime-logs/`
   - Python 的 `__pycache__/`
4. 不要删除或改名 `render.yaml`、`agent-service/requirements-render.txt` 和 `agent-service/knowledge/raw/`，除非同时修改部署方式。
5. Render 服务必须按 **Blueprint / Python Web Service** 创建，不能创建成 Node.js 服务。日志中不应出现 `yarn`、`yarn start` 或缺少 `package.json`。
6. 未经允许不要使用 Docker。本项目的 Render 部署不需要 Docker。

## 二、文件职责

| 文件或目录 | 用途 | 修改后的影响 |
| --- | --- | --- |
| `index.html` | 页面结构、文字与助手入口 | 自动重新部署网页 |
| `styles.css` | 字体、布局、响应式设计与动画 | 自动重新部署网页 |
| `scripts.js` | 页面交互、时间轴、测验和聊天流 | 自动重新部署网页 |
| `Figure/` | 时间轴 SVG 图片 | 自动重新部署网页资源 |
| `agent-service/app/` | FastAPI、Agent、检索与安全逻辑 | 自动重新部署后端 |
| `agent-service/knowledge/raw/` | 线上知识库 Markdown | 提交后线上检索内容更新 |
| `agent-service/requirements-render.txt` | Render Python 依赖 | Render 下次部署重新安装依赖 |
| `render.yaml` | Render Blueprint 和环境变量声明 | 修改后需要在 Render 中同步 Blueprint |

## 三、修改后的本地检查

在项目根目录运行：

```powershell
cd "F:\XML作业\第一次作业\XML和网页代码\网页"

node --check scripts.js
& "D:\xml-agent-venv\Scripts\python.exe" -m compileall -q agent-service\app
```

检查 Render 使用的轻量知识检索：

```powershell
$env:PYTHONPATH = "agent-service"
$env:RAG_BACKEND = "lexical"
& "D:\xml-agent-venv\Scripts\python.exe" -c "from app.rag.retriever import get_retriever; rows=get_retriever().search('什么是多模态'); assert rows; print(rows[0]['section'])"
```

模拟 Render 单服务运行：

```powershell
$env:RAG_BACKEND = "lexical"
& "D:\xml-agent-venv\Scripts\uvicorn.exe" app.main:app --app-dir agent-service --host 127.0.0.1 --port 8002
```

打开 `http://127.0.0.1:8002/`，至少检查：

- 首页、字体、图片和时间轴是否正常；
- 桌面端和移动端布局是否溢出；
- 知识库助手能否回答问题；
- 思考进度、正文和引用资料是否正常显示；
- `http://127.0.0.1:8002/health` 是否返回 `status: ok`。

## 四、提交并触发 Render 更新

Render 已配置从 GitHub 的 `main` 分支自动部署。完成修改和检查后运行：

```powershell
git status
git add .
git diff --cached --check
git commit -m "说明本次修改内容"
git push origin main
```

推送成功后，在 Render 服务的 **Events** 或 **Logs** 页面观察状态：

```text
Cloning -> Building -> Deploying -> Live
```

状态变成 `Live` 后再打开线上网站并强制刷新。免费实例休眠后，第一次访问可能需要等待几十秒。

### 推送失败时

本机 Git 全局配置过 `127.0.0.1:7897` 代理。如果代理软件未运行，可能出现：

```text
Failed to connect to github.com port 443 via 127.0.0.1
```

优先启动对应代理软件后重试。不要把 GitHub Token 写入远程仓库地址或命令记录。

## 五、Render 配置更新规则

- 只修改页面、Python、知识文档或依赖：推送 `main` 后通常会自动部署。
- 修改 `render.yaml`：推送后进入 Render 的 Blueprint 页面，执行 **Sync Blueprint**。
- 修改 API Key：进入 Render 服务的 **Environment**，修改 `DEEPSEEK_API_KEY` 后保存并重新部署。
- 自动部署没有触发：点击 **Manual Deploy -> Deploy latest commit**。
- 部署日志出现 `yarn start`：说明服务被创建成 Node.js。删除错误服务，然后通过仓库的 `render.yaml` 重新创建 Blueprint。

Render 正确配置应为：

```text
Runtime: Python
Build Command: pip install -r agent-service/requirements-render.txt
Start Command: uvicorn app.main:app --app-dir agent-service --host 0.0.0.0 --port $PORT
Health Check: /health
```

## 六、提交前的隐私检查

运行以下命令，确认真实 `.env` 和本地数据仍被忽略：

```powershell
git check-ignore -v agent-service/.env hf-cache milvus-data runtime-logs
git status --short
```

在 `git status` 中只能看到准备公开的源码和文档。发现 API Key、`.env`、缓存、数据库或日志进入暂存区时，必须先停止提交并排除它们。

## 七、上线后的最低验收标准

每次 Render 部署完成后都要验证：

1. 网站首页能打开，静态资源没有 404。
2. `/health` 返回正常，并显示 `rag_backend: lexical`。
3. 助手回答“你是谁”时保持“AI 通识局知识库助手 / 知识导览员”的角色。
4. 知识类问题能返回自然回答和资料来源。
5. 浏览器控制台没有持续报错。
6. GitHub 仓库中不存在真实 API Key 或 `agent-service/.env`。
